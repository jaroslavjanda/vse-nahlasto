export function Tickets({ tickets }) { const [LikedRequest, {data}] = useMutation(LIKE_MUTATION); return ( <div style={{ textAlign: 'center' }}> <div> <CardColumns> {tickets.map((item) => ( <CardsTicket key={item.ticket_id} item={item} like={item.likes_count} requestSendLike={LikedRequest} /> ))} </CardColumns> </div> </div> ); }